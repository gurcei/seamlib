#OUTPUT "HISCORE"'--------.DECLARES'--------#DECLARE K, NAME$(3), SCORE(3), LEVEL$(3), A$'-------.DEFINES'-------#DEFINE À≈ŸØ≈”√¡–≈ = CHR$(27)'----.MAIN'----  GOSUB INIT  GOSUB LOADØCURRENTØHIGHØSCORES  GOSUB DRAWØHISCOREØTITLE  GOSUB DRAWØHISCOREØLIST  DO    GET KEY A$    IF A$ = À≈ŸØ≈”√¡–≈ THEN DLOAD "SEAMLIB"  LOOP  END'----.INIT'----  LEVEL$(0) = "≈XTREME"  LEVEL$(1) = "ƒIFFICULT"  LEVEL$(2) = "ÕEDIUM"  LEVEL$(3) = "≈ASY"  RETURN'------------------------.LOADØCURRENTØHIGHØSCORES'------------------------  DOPEN #2, "SCORES", R, U8  FOR K = 0 TO 3    INPUT #2, NAME$(K)    INPUT #2, SCORE(K)  NEXT K  DCLOSE #2  RETURN'------------------.DRAWØHISCOREØTITLE'------------------  PRINT "HIGH-SCORES:"  PRINT "==========="  RETURN'-----------------.DRAWØHISCOREØLIST'-----------------  FOR K = 0 TO 3    PRINT LEVEL$(K) ; ": "; NAME$(K); " - SCORE: "; SCORE(K)  NEXT K  RETURN